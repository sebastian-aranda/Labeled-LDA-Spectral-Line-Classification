#Fix1
- Models trained with alma_band_6 rounds frequency, but the original models truncates the frequency to the lowest integer. For that "llda_input_parser_CASA.fix1.py" is created

#Fix2
- Models trained with alma_band_6 rounds frequency, but the original models truncates the frequency to the lowest integer. For that "llda_input_parser.fix1.2.py" is created
